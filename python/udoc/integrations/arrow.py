"""udoc.integrations.arrow -- pyarrow integration (placeholder+).

This module is a placeholder to lock the integration extension
pattern: the slot exists, the extras_require entry exists, and downstream
docs can reference the install instruction now. Real implementation lands
post-.

To install when ready:

    pip install udoc[arrow]
"""
